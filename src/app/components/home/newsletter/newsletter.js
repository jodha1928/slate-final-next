"use client";

import styles from "./newsletter.module.scss";
import Image from "next/image";

export default function Newsletter() {
  return (
    <div className={styles.newsletter} ref={sectionRef}>
      <div className={`container`}>
       
      </div>
    </div>
  );
}
